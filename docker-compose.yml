version: '3.8'

services:
  api:
    build: .
    ports:
      - "9000:9000"
    environment:
      - PORT=9000
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_SERVER=${DB_SERVER}
      - DB_DATABASE=${DB_DATABASE}
